@model Lazyfitness.Models.postArea
@using Lazyfitness.Models;
@{
    Layout = "~/Views/Shared/_backStageMainStyle.cshtml";
    ViewBag.title = "论坛分区管理";
}
@{ 
    postArea[] forumAreaList = ViewBag.forumAreaList as postArea[];
}
<!--/sidebar-->
<div class="main-wrap">
    <div class="crumb-wrap">
        <div class="crumb-list">
            <i class="icon-font">🔈</i><span>欢迎使用TPAS后台管理系统</span>
        </div>
    </div>

    <div class="result-wrap">
        <div class="result-title">
            <h1>可选操作</h1>
        </div>
        <div class="result-content">
            <div class="short-wrap">
                @*@Html.ActionLink("增加分区", "forumAreaAdd", "forumManagement")
                @Html.ActionLink("删除分区", "forumAreaDelete", "forumManagement")
                @Html.ActionLink("修改分区信息", "forumAreaSearch", "forumManagement")*@
                @Html.ActionLink("返回上一级", "forumAreaIndex", "forumManagement")
            </div>
        </div>
    </div>
    <div class="result-content">
        <form action="~/backStage/forumManagement/forumAreaDelete" method="post" autocomplete="off">
            <table class="insert-tab">
                <tr><th></th><td class="show-title">删除分区</td></tr>
                <tr>
                    <th>
                        分区名称
                    </th>
                    <td>
                        <select name="areaId">
                            @{
                                foreach (var forumArea in @forumAreaList)
                                {
                                    <option class="dropdown-list" value="@forumArea.areaId">@forumArea.areaName</option>
                                }
                            }
                        </select>
                    </td>
                </tr>
                <tr>
                    <th></th>
                    <td>
                        <input class="btn-submit show-border" type="submit" id="postData" name="postData" value="删除" />
                    </td>
                </tr>
            </table>
        </form>
    </div>
</div>